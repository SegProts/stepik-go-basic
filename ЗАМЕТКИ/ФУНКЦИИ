func multiply(x int, y int) int {
    return x * y
}
Объявление функции состоит из следующих частей:

Если первая буква заглавная, то функция экспортируемая (публичная) и
доступна в других пакетах. 
Если с маленькой буквы используются только в рамках текущего пакета
------------------------------------------------------------------------------
Из одной функции можно возвращать несколько значений. 
Чаще всего это используется для возвращения ошибок:

func divide(x, y int) (int, error) {
    if y == 0 {
        return 0, errors.New("cannot divide on zero")
    }

    return x / y, nil
}

Возвращаемые значения могут быть именованными:

func multiply(x, y int) (message string, res int) {
	res = x * y
	message = "Сообщение"

    //неявно возвращает оба значения или можно написать return message, res 
	return 
}

Получаем именованые результаты функции
(_ - ипользуется, если какой-то результат не нужен)

func main() {
	_, result := multiply(4, 10)
	fmt.Print(result)
}





